<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ghostchu.peerbanhelper.databasent.mapper.java.AlertMapper">
    <update id="markAllAsRead">
        UPDATE alert SET read_at = NOW() WHERE read_at IS NULL
    </update>
    <update id="markAsRead" parameterType="string">
        UPDATE alert SET read_at = NOW() WHERE identifier = #{identifier} AND read_at IS NULL
    </update>
</mapper>
