<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ghostchu.peerbanhelper.databasent.mapper.java.PCBAddressMapper">
    <insert id="upsert" parameterType="com.ghostchu.peerbanhelper.databasent.table.PCBAddressEntity">
        ALTER TABLE pcb_address
        ADD UNIQUE KEY uk_pcb_address_biz (ip, port, torrent_id, downloader);

        INSERT INTO pcb_address (
        ip,
        port,
        torrent_id,
        last_report_progress,
        last_report_uploaded,
        tracking_uploaded_increase_total,
        rewind_counter,
        progress_difference_counter,
        first_time_seen,
        last_time_seen,
        downloader,
        ban_delay_window_end_at,
        fast_pcb_test_execute_at,
        last_torrent_completed_size
        ) VALUES (
        ?,
        ?,
        ?,
        ?,
        ?,
        ?,
        ?,
        ?,
        ?,
        ?,
        ?,
        ?,
        ?,
        ?
        ) AS new
        ON DUPLICATE KEY UPDATE
        last_report_progress = new.last_report_progress,
        last_report_uploaded = new.last_report_uploaded,
        tracking_uploaded_increase_total = new.tracking_uploaded_increase_total,
        rewind_counter = new.rewind_counter,
        progress_difference_counter = new.progress_difference_counter,
        first_time_seen = new.first_time_seen,
        last_time_seen = new.last_time_seen,
        ban_delay_window_end_at = new.ban_delay_window_end_at,
        fast_pcb_test_execute_at = new.fast_pcb_test_execute_at,
        last_torrent_completed_size = new.last_torrent_completed_size;
    </insert>

</mapper>
