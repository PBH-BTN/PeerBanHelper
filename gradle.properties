# Project properties
org.gradle.jvmargs=-Xmx2G -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true

# Java version
javaVersion=25

# install4j home (can be overridden via environment variable or command line)
install4jHome=/opt/install4j
